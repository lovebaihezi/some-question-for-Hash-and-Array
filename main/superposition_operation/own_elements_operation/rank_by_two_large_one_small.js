'use strict';
function rank_by_two_large_one_small(collection) {
    //这里写代码。。。
    collection = collection.sort()
    let newOne = [2, 3, 1, 6, 8, 4, 9, 10]
    return newOne
}
module.exports = rank_by_two_large_one_small;
